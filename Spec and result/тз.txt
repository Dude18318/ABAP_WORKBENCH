Массовая выборка из QMEL
Собрать все QMNUM в отдельную таблицу и выполнить один SELECT … FOR ALL ENTRIES IN @lt_qmnum (или RANGE). Результат сохранить в хэш-таблицу для быстрого доступа по QMNUM.

Построить хэш‑таблицу блокированных QMNUM из логов
Один раз пройти mt_bal_log_db_messages, отфильтровать сообщения IM 416 E, собрать уникальные msgv1 в HASHED TABLE. После этого все проверки сводятся к чтению хэш-таблицы (READ TABLE), а не к LOOP … WHERE.

Сократить обращения к mt_bal_log_db_messages
Хранить её как SORTED или HASHED с ключом (msgid, msgno, msgty, msgv1), чтобы ускорить поиск.

Вообще изучи контекст нашего проекта - там много полезного по этому поводу.
